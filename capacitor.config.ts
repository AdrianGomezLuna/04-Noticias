import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.noticias',
  appName: 'noticias',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
